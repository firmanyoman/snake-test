# snakes-oc

Simple Snake game built with OpenCode.

## Run

Open `index.html` in browser.

Or serve directory with any static file server, for example:

```bash
python3 -m http.server
```

Then open shown local URL in browser.

## Controls

- Arrow keys: move snake
- Restart button: start new game after game over
